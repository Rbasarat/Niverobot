version: '3.7'

services:
  Niverobot.WebApi:
    image: niverobot
    build:
      context: .
      dockerfile: ./Niverobot.WebApi/Development.Dockerfile
    volumes:
      - ./Logs:/app/Logs
    ports: 
      - 5000:80
    depends_on:
      - Niverobot.SQL
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  Niverobot.SQL:
    image: "mcr.microsoft.com/mssql/server:2019-latest"
    volumes:
      - ./Db:/sql
    environment:
      SA_PASSWORD: "ad6QVLqE@W"
      ACCEPT_EULA: "Y"
      
  Niverobot.Dateparser:
    image: dateparser
    build: 
      context: .
      dockerfile: ./Niverobot.Dateparser/Dockerfile
    ports:
      - "3001:50051"
