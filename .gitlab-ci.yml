image: ubuntu:latest

variables:
  # Use TLS https://docs.gitlab.com/ee/ci/docker/using_docker_build.html#tls-enabled
  DOCKER_TLS_CERTDIR: "/certs"
  IMAGE_NAME: niverobot

stages:
    # - build
    # - release
    - deploy

# build:
# image: docker:19.03.1
# services:
#   - docker:19.03.1-dind
#   before_script:
#     - cd Nivero
#   script:
#     - docker build -t $CI_REGISTRY/niverhawk/$IMAGE_NAME -f Niverobot.WebApi/Dockerfile .
#   stage: build
#   except:
#     - master

# release:
#   only:
#     - master
#   before_script:
#     - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
#     - cd Niverobot
#   script:
#     - docker build -t $CI_REGISTRY/niverhawk/$IMAGE_NAME -f Niverobot.WebApi/Dockerfile .
#     - docker push $CI_REGISTRY/niverhawk/$IMAGE_NAME
#     - ssh root@server docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY && touch test
#   after_script:
#     - docker logout $CI_REGISTRY
#   stage: release

deploy:
  # only:
  #   - master
  stage: deploy
  before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client git -y )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  script:
    - ssh niverhawk@51.83.74.19 docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY && touch test
