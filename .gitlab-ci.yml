image: docker:latest
services:
  - docker:dind

variables:
  DOCKER_TLS_CERTDIR: "/certs"
  DOCKER_DRIVER: overlay2

before_script:
  - cd Niverobot

services:
  - docker:dind

stages:
    - build
    #- test
    # - deploy

# before_script:
#   - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY

build:
  script:
    - docker build -t test -f Niverobot.WebApi/Dockerfile .
  stage: build
  except:
    - master

# deploy:
#   # only:
#   #   - master
#   script:
#     - docker build -t ${CI_REGISTRY}/Niverobot:latest -f Niverobot.WebApi/Dockerfile --build-arg BotConfiguration__BotToken=$BotConfiguration__BotToken .
#     - docker push ${CI_REGISTRY}/Niverobot:latest
#   after_script:
#     - docker logout ${CI_REGISTRY}
#   stage: build
#   tags: 
#     - docker