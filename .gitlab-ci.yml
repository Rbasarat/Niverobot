image: docker:19.03.6

services:
  - docker:19.03.6-dind

# Disable tls because dind.
variables:
   DOCKER_TLS_CERTDIR: "/certs"

stages:
    - build
    #- test
    # - deploy

before_script:
  - docker login -u Niverhawk -p $accessToken ${CI_REGISTRY}

build:
  script:
    - docker build -t ${CI_REGISTRY}/${CI_PROJECT_PATH}:dev -f Niverobot.WebApi/Dockerfile .
  stage: build
  except:
    - master

# deploy:
#   # only:
#   #   - master
#   script:
#     - docker build -t ${CI_REGISTRY}/Niverobot:latest -f Niverobot.WebApi/Dockerfile --build-arg BotConfiguration__BotToken=$BotConfiguration__BotToken .
#     - docker push ${CI_REGISTRY}/Niverobot:latest
#   after_script:
#     - docker logout ${CI_REGISTRY}
#   stage: build
#   tags: 
#     - docker